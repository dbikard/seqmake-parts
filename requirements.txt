# Pinned for reproducibility: rdflib's Turtle serialization and biopython's
# pairwise aligner must be stable so the committed catalog.ttl and the UniProt
# variant classification don't drift between environments / CI runs.
biopython==1.87
requests==2.33.1
mkdocs-material==9.7.6
rdflib==6.3.2
pyshacl==0.28.1
jsonschema==4.26.0
