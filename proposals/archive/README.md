# `proposals/archive/` — implemented / closed proposals

Design documents whose proposals have been built. Kept as design rationale (the
*why* behind the implementation), not as pending work.

- **`unified-add-part.md`** — the design for the unified `/add-part` researched-authoring
  capability. Substantially implemented; the **live spec** is [`AUTHORING.md`](../../AUTHORING.md),
  the driver is `.claude/commands/add-part.md`, and the engine is
  `.claude/workflows/annotate-part.js`.
